import { useState } from 'react';
import { Button, Input } from 'gdg-ui';

// ✅ TASK: Build the "Join the Bootcamp" Form
// Goal: Handle form submission and conditional success message.
export default function RegistrationForm() {
  const [name, setName] = useState('');
  const [user, setUser] = useState('');

  return (
    <div>
       {/* 6. Build this together... */}
       <p style={{ color: '#94a3b8' }}>RegistrationForm Skeleton... ready for code!</p>
    </div>
  );
}
