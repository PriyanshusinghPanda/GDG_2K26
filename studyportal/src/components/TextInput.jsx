import { useState } from 'react';
import { Input } from 'gdg-ui';

// ✅ TASK: Build the "Live Text Input"
// Goal: An input field that updates a preview text live.
export default function TextInput() {
  const [text, setText] = useState('');

  return (
    <div>
       {/* 2. Build this together... */}
       <p style={{ color: '#94a3b8' }}>Input Skeleton... ready for code!</p>
    </div>
  );
}
